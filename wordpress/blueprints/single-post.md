# Single post blueprint

Order: Rank Math breadcrumb → article header → metadata → featured image → article content → author/share area → related Posts → CTA. Article title is the only H1; body uses H2 then H3. Hero image 16:9. Desktop content/sidebar is 8/4 when needed; mobile content first. WordPress owns article data; Rank Math owns Article/Breadcrumb metadata and schema.
