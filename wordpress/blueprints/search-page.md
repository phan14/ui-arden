# Search results blueprint

Order: search Hero with query → search form → grouped dynamic results → pagination → empty-state suggestions → CTA. One H1 such as “Kết quả tìm kiếm”; result titles H2/H3. Cards 16:10 when thumbnails exist. Desktop 3 columns, tablet 2, mobile 1. Use native WordPress search/query data and escaped query output.
