# Home content diff

All headings, paragraphs, metrics, testimonials, FAQs, card data and CTA labels are restored from `HomePage.tsx` and `siteData.ts`.

Data-backed titles are stored in the same title case as React. Their visual uppercase treatment is applied only by scoped CSS. Step/capability numbers are separate decorative elements as in the React components.

Image tokens are implementation placeholders and do not change text content; runtime fallbacks use the exact React reference URLs.

Result: **NO UNAPPROVED CONTENT DIFFERENCE**.
