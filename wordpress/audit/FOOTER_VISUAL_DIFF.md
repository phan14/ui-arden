# Footer Visual Diff

Status: **FAIL — MATERIAL DIFFERENCES REMAIN**.

The footer remains global and reusable; it is not duplicated into imported pages. The existing `arden-footer` UX Block contains the expected four-column content pattern and bottom copyright row.

Remaining differences:

- Arden logo asset is not configured;
- contact placeholders (`ARDEN_PHONE`, `ARDEN_EMAIL`, `ARDEN_ADDRESS`, `ARDEN_OPENING_HOURS`) require verified production values;
- social links still contain placeholders;
- spacing, mobile stacking and the reference pre-footer CTA have not reached 1:1 visual sign-off.

