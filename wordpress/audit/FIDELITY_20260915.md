# React → WordPress fidelity work, 2026-09-15

## Baseline and rollback

- Reference: `src`, rendered at `http://localhost:3000/`.
- Target: `C:/xampp/htdocs/mytest`, `http://localhost/mytest/`.
- Active theme: `flatsome-child` 2.0.0, parent Flatsome 3.17.7.
- Repository child theme: 2.0.1; ten files differ from the running theme.
- Existing working-tree modifications predate this session and are preserved.
- Before changes, both child-theme directories were copied to `wordpress/backups/fidelity-20260915/`.
- No database changes are part of the initial theme synchronization.
- Before screenshots: `task11-baseline-20260915/` (Home at 1440, 1024, 768, 390; About at 1440 and 1024). This older capture script does not reliably settle lazy images; these are diagnostic screenshots, not acceptance evidence.

## Implementation mapping before synchronization

| React source | Existing WordPress implementation |
| --- | --- |
| `App`, `Container`, global CSS | Flatsome layout plus scoped `assets/css/arden.css` |
| `Header`, `Footer` | Flatsome Header Builder / UX Blocks, styled by child theme |
| Home hero and reusable sections | Editable page/UX Block content with child-theme styles |
| About and service/information pages | Imported page content with `arden-react-page`, utility styles and native interactive elements |
| Projects, News, Search, Case Study | Child-theme PHP templates, native WordPress content and queries |
| Pricing calculator | `arden_pricing_calculator` shortcode and `native-interactions.js` |
| SVG icons | `arden_icon` shortcode and shared icon paths |
| Forms | Contact Form 7 and `forms.css` |
| Navigation, tabs, filters, mobile interactions | `native-interactions.js` |

Initial file scope: `archive-project.php`, `functions.php`, `page-tim-kiem.php`, `page-tin-tuc.php`, `single-project.php`, `style.css`, `assets/css/arden.css`, `assets/css/react-utility-compat.css`, `assets/js/native-interactions.js`, `inc/shortcodes.php` in the active child theme. Synchronize existing repository work after syntax checks, then evaluate remaining differences against React.

## Changes completed

- Synchronized the repository child theme 2.0.1 to the local runtime after backing up both copies.
- Restored Home and its seven UX Blocks from the source-mapped imports. This removed stale inline CSS, an extra wrapper section and the incorrect `100 áo/mẫu` value.
- Fixed the utility CSS generator so all 443 utility selectors are scoped below `.arden-react-page`; regenerated the compatibility stylesheet with zero unscoped rules.
- Restored the shared 12-column grid and card padding removed during the React-to-UX-Builder conversion.
- Added route-scoped layout layers for About, Manufacturing and Contact.
- Reconstructed the six Manufacturing capability cards in the browser from their editable UX Builder sibling fields.
- Published local Page 87 (`nang-luc-san-xuat`) with user authorization. Its complete pre-change record is backed up.

## Final visual/runtime verification

- Evidence: `wordpress/audit/fidelity-20260915/final/` plus focused post-fix captures in `contact-fixed/`, `manufacturing-cards/` and `continue-about/`.
- Matrix: 21 routes at 1440×900, 768×1024 and 390×844 = 63 browser cases.
- Result: zero horizontal overflow, broken images, page errors and failed requests.
- Intended HTTP results: all content routes 200; the missing-page route 404.
- Focused structural test for Search, News, Projects and Fabric Guide: PASS.
- Interaction regression: runtime 84, responsive 81, visual 84, content 80, structure 80, interactions 84.
- PHP syntax: 14/14 PASS. JavaScript syntax, TypeScript and Vite production build: PASS.
- WordPress Admin and edit URLs return the expected 302 to login when logged out. Active theme remains `flatsome-child` 2.0.1; local indexing remains disabled.

## Remaining differences

No 1:1 claim is made. The remaining visible differences are:

- Category archive at all widths is substantially shorter because WordPress has fewer real posts than the React fixture. Correcting this requires creating/publishing additional production-approved posts and images; CSS cannot supply missing content.
- Search height varies with the WordPress result set, especially at 768 and 390, for the same data reason.
- Manufacturing remains about 10–14% taller, concentrated in Factory, Capabilities and Process card copy flattened into editable UX Builder fields.
- Services remains about 14–15% taller at 1440 and 390 because its long editable model/process copy wraps differently from the React component groups.
- Careers remains about 13–22% shorter because the local WordPress job data does not contain every React fixture entry.
- About is about 5–7% taller after the structural fixes; the residual is distributed across native Flatsome text wrappers and card copy.
- Contact is corrected from a broken narrow desktop column to a 5/7 grid. Residual document-height deltas are +260px at 1440, +121px at 768 and -110px at 390.

These data-dependent differences should be closed only after final Posts, Categories and job listings are approved. The theme and layout layer now render the available WordPress content without the major structural failures observed in the baseline.
